<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ChatClient.NewGroupChatPage">
    <ContentPage.Content>
		<StackLayout Orientation="Vertical" VerticalOptions="Center">
			<ListView x:Name="list" VerticalOptions="Center">
				<ListView.ItemTemplate>
					<DataTemplate>
						<Entry x:Name="usernameEntry" HorizontalOptions="FillAndExpand" Placeholder="Enter username here"/>
						<Button x:Name="usernameButton" Text="Remove" Clicked="RemoveEntry"/>
					</DataTemplate>
				</ListView.ItemTemplate>
			</ListView>
			<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="*"/>
					<ColumnDefinition Width="*"/>
				</Grid.ColumnDefinitions>
				<Button Text="Create Chat" Clicked="CreateGroupChat"
					HorizontalOptions="End" Grid.Column="0"/>
				<Button Text="Create Chat" Clicked="AddEntry"
					HorizontalOptions="End" Grid.Column="1"/>
			</Grid>
		</StackLayout>
	</ContentPage.Content>
</ContentPage>